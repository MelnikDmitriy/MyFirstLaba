/* Написать функцию, принимающую два числа и любую операцию над этими числами (сложение, вычитание).
В функции выполнить эту операцию и вернуть результат */

const calculate = (a, b ,c) => {
    console.log(`${a + b + c}`);
};

const number = 5;
const numberTwo = 10;
const operation = '-';

calculate(number, numberTwo, operation);